<!--{% set first_iteration = true %}
    {% for user in users %}
        {% if first_iteration %}
            <h4>{{ user.unique_code }}</h4>
            {% set first_iteration = false %}
        {% endif %}
    {% endfor %}-->
      <!-- {% for user in users %}
      
          <h4>{{ user.unique_code }}</h4>
          {% break %}
    {% endfor %}-->